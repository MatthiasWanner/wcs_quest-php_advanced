<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Twig</title>
    <link rel="icon" href="/assets/images/favicon.png">
    <link rel="stylesheet" href="/assets/css/styles.css">
</head>

<body>
    <h1>Hello {{ name }} !</h1>
    <h2>{{ message }}</h2>
    <p>👇 Here is a non-exhaustive list of music instruments, passed with twig render variables 🚀:</p>
    <ul>
        {% for product in products %}
            <li>
                {{ product }}
            </li>
        {% endfor %}
    </ul>
</body>
</html>